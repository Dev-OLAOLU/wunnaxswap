# Deploy

Primary host: **Vercel** (import this GitHub repo).

1. https://vercel.com/new → Import `Dev-OLAOLU/wunnaxswap`
2. Framework: Other · Build: (empty) · Output: `.`
3. Every push to `main` auto-deploys.

Netlify configs are legacy; ignore `netlify.toml` for new deploys.
