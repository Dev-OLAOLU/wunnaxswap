# Supabase (deprecated for Wunnaxswap)

Wunnaxswap now uses **Firebase** (free Spark plan) instead of Supabase.

See: **[FIREBASE_BACKEND.md](./FIREBASE_BACKEND.md)**

The old SQL file `supabase/APPLY_IN_DASHBOARD.sql` is kept only as a reference and is **not** required.
